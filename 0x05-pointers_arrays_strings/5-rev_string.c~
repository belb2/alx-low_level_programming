#include<stdio.h>
void rev_string(char *s){
  int i=0;
  while(s[i] != '\0')
    i++;
  while(s[i] != '\0'){
    _putchar(s[i]);
    i--;
  }
}

void main()
{
  char *s;
  s= "my life is you";
  rev_string(char *s);
}
